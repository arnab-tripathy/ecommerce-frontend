import { AddNewAddressRequest } from './add-new-address-request';

describe('AddNewAddressRequest', () => {
  it('should create an instance', () => {
    expect(new AddNewAddressRequest()).toBeTruthy();
  });
});
